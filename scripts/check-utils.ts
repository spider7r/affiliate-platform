import { createClient } from '@/utils/supabase/server'

// This file is just a reference request to verify we can import createClient from server utils
// It confirms the import path exists.
console.log('Server utils import path is valid.')
